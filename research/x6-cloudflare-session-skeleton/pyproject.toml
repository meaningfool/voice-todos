[project]
name = "x6-cloudflare-session-skeleton"
version = "0.1.0"
description = "Spike X6: local Cloudflare Worker + Durable Object session skeleton"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
